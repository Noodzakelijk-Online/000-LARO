on_http_request:
  - expressions:
      - "req.url.path == '/laro' || req.url.path.startsWith('/laro/')"
    actions:
      - type: forward-internal
        config:
          url: https://laro.internal
