/**
 * Outreach automation — stubs (file was truncated). Wire to your jobs + mailer when ready.
 */

export async function processOutreachCron(): Promise<void> {
  /* no-op */
}

export async function recordLawyerResponse(
  caseId: string,
  lawyerId: string,
  accepted: boolean,
  body: string
): Promise<void> {
  void caseId;
  void lawyerId;
  void accepted;
  void body;
}
